name: barberapp_yucatan
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter

  # ✅ ICONOS
  cupertino_icons: ^1.0.8

  # ✅ NETWORKING
  http: ^1.2.0

  # ✅ ALMACENAMIENTO SEGURO
  flutter_secure_storage: ^9.0.0

  # ✅ ALMACENAMIENTO LOCAL (NECESARIO PARA SharedPreferences)
  shared_preferences: ^2.2.2

  # ✅ FUENTES
  google_fonts: ^6.1.0

  # ✅ ANIMACIONES
  flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true